name: GitHub 이슈 생성 시 Jira Task 자동 생성 및 키 자동 연결

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  create-issue:
    name: Create Jira issue and comment issue key
    runs-on: ubuntu-latest
    steps:
      - name: Jira 로그인
        uses: atlassian/gajira-login@v3
        env:
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
          JIRA_USER_EMAIL: ${{ secrets.JIRA_USER_EMAIL }}

      - name: Jira 이슈 생성
        id: create_jira
        uses: atlassian/gajira-create@v3
        with:
          project: DEEP
          issuetype: Task
          summary: '${{ github.event.issue.title }}'
          description: |
            GitHub Issue: [${{ github.event.issue.title }}](${{ github.event.issue.html_url }})
            작성자: ${{ github.event.issue.user.login }}
            본문:
            ${{ github.event.issue.body }}

      - name: 생성된 Jira 이슈키를 Github 이슈에 코멘트로 기록
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `🔗 **Jira 이슈가 생성되었습니다:** [${{ steps.create_jira.outputs.issue }}](${{ secrets.JIRA_BASE_URL }}/browse/${{ steps.create_jira.outputs.issue }})`
            })
